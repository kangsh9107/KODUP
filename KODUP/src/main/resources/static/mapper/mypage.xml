<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kodup.mypage.MypageMapper">
	<!-- 회원 정보 -->
	<select id='info' parameterType="String" resultType="com.kodup.mypage.MypageVo">
		SELECT id, nickname, email, gender, age, profile_img FROM member WHERE id= #{_parameter}
	</select>

	<!-- 회원정보 업데이트 -->
	<update id='mypage_memberinfo_update_complete' parameterType="com.kodup.mypage.MypageVo" >
		UPDATE member SET nickname = #{nickname }, email= #{email }, gender= #{gender }, 
			   			  age= #{age }, profile_img = #{profile_img}
		WHERE id = #{id} 
	</update>
	
	<!-- 회원정보 수정..  -->
	<update id='update' parameterType='com.kodup.mypage.MypageVo'>
		UPDATE member SET nickname = #{nickname }, email= #{email }, gender= #{gender }, 
			   			  age= #{age }, profile_img = #{profile_img}
		WHERE id = #{id} 
	</update>
	
	<!-- <insert id='attUpdate' parameterType='com.kodup.mypage.MypageVo'>
		insert into - 테이블이 없음..
		insert into member(profile_img)
		
    	insert into member(profile_img)
        values (#{profile_img}) where id = #{id}
	</insert>	 -->
	
	<!-- <delete id='attDelete' parameterType="String">
		delete from boardAtt where sysFile in(
			<foreach collection='attList' item='vo' separator=','>
				#{file}
			</foreach>
	)	
	</delete> -->
	
	
	<!-- 회원 탈퇴 -->
	<delete id='member_quit' parameterType='String'>
		delete from member where id= #{_parameter}
	</delete> 
	
	
	
	<!-- resultType='com.kodup.mypage.MypageQuitVo' -->
	
</mapper>